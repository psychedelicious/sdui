import { Container } from '@chakra-ui/react';
import Form from './features/form/form';

const App = () => {
    return (
        <Container>
            <Form />
        </Container>
    );
};

export default App;
